import { Router } from 'express';

const authRouter = Router();

authRouter.post('/authentificate_with_google', (req, res) => {});
authRouter.post('/authentificate_with_email_and_password', (req, res) => {});

export default authRouter;
